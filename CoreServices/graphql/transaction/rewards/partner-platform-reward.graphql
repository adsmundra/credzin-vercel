interface PartnerPlatformReward implements Reward {
    id: ID,
    name: String
    rewardType: RewardType,
    rewardValue: [RewardValue]
}

type GyfterPartnerPlatformReward implements PartnerPlatformReward & Reward {
    id: ID,
    name: String,
    rewardType: RewardType,
    rewardValue: [RewardValue],
}

type SmartBuyPartnerPlatformReward implements PartnerPlatformReward & Reward {
    id: ID,
    name: String,
    rewardType: RewardType,
    rewardValue: [RewardValue],
}

